name: Update references.json from journals.bib

on:
  push:
    paths:
      - journals.bib

jobs:
  update-references:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # allow committing back to the repo

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Cache pip dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Set up Python and install manubot
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install manubot
      run: pip install manubot

    - name: Cache Pandoc installer
      id: pandoc-cache
      uses: actions/cache@v4
      with:
        path: pandoc-3.7.0.2-1-amd64.deb
        key: pandoc-3.7.0.2-deb

    - name: Download Pandoc installer (if not cached)
      if: steps.pandoc-cache.outputs.cache-hit != 'true'
      run: curl -LO https://github.com/jgm/pandoc/releases/download/3.7.0.2/pandoc-3.7.0.2-1-amd64.deb

    - name: Install Pandoc
      run: sudo dpkg -i pandoc-3.7.0.2-1-amd64.deb

    - name: Generate references.json
      run: |
        manubot cite $(grep '^@' journals.bib | sed -E 's/@[^{]+\{([-A-Za-z0-9_]+),/\1/') \
          --bibliography journals.bib --format csljson > references.json

    - name: Commit and push references.json
      run: |
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git add references.json
        git diff --cached --quiet || git commit -m "Update references.json from journals.bib"
        git push
